rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /menuItems/{id} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == "5RyKdWd5lSQ9GYQxPtSeUtcCbUl2";
    }

    match /orders/{id} {
      allow create, read: if true;

      // DEMO MODE:
      // Payment marks order as PAID from the client after server-side signature verification.
      // For production, update orders on server using Firebase Admin SDK and make this admin-only.
      allow update: if true;
    }

    match /ratings/{id} {
      allow create, read: if true;
      allow update, delete: if false;
    }
  }
}
